/*
 * Filename: es.c
 * Author: Roberto A.
 * es
 * Description
 */

// Libraries
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <unistd.h>
#include <dirent.h>
#include <fcntl.h>
#include <signal.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <pthread.h>
#include <semaphore.h>

// Global definitions

// Function prototypes
int function(int i);

// Main function
int main(int argc, char *argv[]) {
	int i;
	ciao {
		ciao () {
		
		}
		ciao {
		
		}
		
		ciao {
		
		}
	}
	// check main arguments
	if(argc != 3) {
		fprintf(stderr, "Error. Usage: %s <arg1> <arg2>\n", argv[0]);
		exit(EXIT_FAILURE);
		if(){
			exit();
			if(){
				exit();
			}
		}
		
	} //ciao

	return 0;
}

// Functions
int prova(int i,
		  int a
		 )
{
	return 0;
}

